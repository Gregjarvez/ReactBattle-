import  React from 'react';const SelectLanguage = (props) => {        let {languages, onclick, selectedLanguage} = props.data;    let list = languages.map((language) => {        return (            <li                style={language === selectedLanguage() ?                    {color: "#19B5FE"}:                    {color: " #6C7A89"}                }                key={language}                onClick={(() => onclick(language))}            >                {language}            </li>        )    });            return (            <ul className="languages">                {list}            </ul>        );    };class Popular extends React.Component {    constructor(props) {        super(props);        this.state = {            selectedLanguage: "All"        }            }    setActiveLink(lang) {        return this.setState({selectedLanguage: lang})    }    getSelectedLanguage() {        return this.state.selectedLanguage;    }    render() {        const languages = ["All", "Javascript", "Ruby", "Java", "Css", "Python"];        this.getSelectedLanguage = this.getSelectedLanguage.bind(this)        const data = {            languages: languages,            onclick: this.setActiveLink.bind(this),            selectedLanguage: this.getSelectedLanguage        }            return (            <div className="languages">                <SelectLanguage data={data}/>            </div>        )    }}console.log("this is just for show");export default Popular;